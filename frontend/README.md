# Frontend

Angular standalone application for the Magic Home Control API.

```powershell
npm install
npm start
```

The application expects the API under `/api`. In development, use an Angular proxy or run the production Compose setup, which routes `/api` through Nginx.
